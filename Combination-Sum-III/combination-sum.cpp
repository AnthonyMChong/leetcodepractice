#include <string>
#include <ostream>
#include <iostream>
#include <vector>

std::vector <std::vector<int>> conbinationSum3(){ //Vectors are sequence containers representing arrays that can change in size.
    std::vector<int> v = {7, 5, 16, 8};
    return v;
}

int main(){

    std::cout << conbinationSum3() << std::endl;
    std::vector <std::vector<int>> = conbinationSum3();
    for (int n : returnV){
        std::cout<< n << std::endl;
    }
}